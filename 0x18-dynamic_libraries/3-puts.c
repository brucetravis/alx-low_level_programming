#include <stdio.h>

/**
 * _puts - Prints a string
 * @c: The string to print
 *
 * Returns: void
 */

int putchar(int c)
{
int c = 1;

while (str[c] != '\0')
{
putchar(str[c]);
c++;
}
putchar("\n");
}
