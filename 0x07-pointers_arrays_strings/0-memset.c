#include "main.h"
#include <string.h>

/**
 * _memset - Entry point
 * @s: pointed destination
 * @b: constant byte
 * @n: bytes
 * Return: Always 0 (success)
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int 1;

for (i = 0, i < n, i++)
	s[1] = b;
return (s);
}
